# Test-only dependencies (kept out of the production image / requirements.txt).
pytest>=8.0
pytest-asyncio>=1.0
httpx>=0.27
passlib[bcrypt]>=1.7.4
bcrypt<4.1  # passlib 1.7.4 reads bcrypt.__about__, removed in 4.1+
ruff>=0.6
